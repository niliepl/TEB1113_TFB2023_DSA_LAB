<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Header -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="16dp"
            android:text="Attendance Tracker"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- Input Section -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Enter Student ID:" />

        <EditText
            android:id="@+id/studentIdInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:hint="e.g. 22011238"
            android:inputType="text" />

        <!-- Algorithm Selection -->
        <RadioGroup
            android:id="@+id/algorithmGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:orientation="horizontal">

            <RadioButton
                android:id="@+id/radioArray"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checked="true"
                android:text="Array" />

            <RadioButton
                android:id="@+id/radioLinkedList"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="24dp"
                android:text="Linked List" />
        </RadioGroup>

        <!-- Action Buttons -->
        <Button
            android:id="@+id/checkInButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Check In" />

        <Button
            android:id="@+id/searchButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Search ID" />

        <!-- Data Structure Visualization -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp">

            <LinearLayout
                android:id="@+id/visualContainer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal" />
        </HorizontalScrollView>

        <!-- Operation Log Section -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Operation Steps:"
            android:textStyle="bold" />

        <!-- Nested ScrollView for operation steps -->
        <!-- Change this part of your XML -->
        <ScrollView
            android:id="@+id/stepScrollView"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:layout_marginTop="8dp"
            android:background="#F5F5F5"
            android:fillViewport="true"
            android:scrollbars="vertical"
            android:overScrollMode="always"
            android:nestedScrollingEnabled="true">

            <TextView
                android:id="@+id/stepDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="12dp"
                android:textColor="#000000"
                android:textSize="14sp"
                android:scrollbars="vertical"
                android:textIsSelectable="true"/>
        </ScrollView>

        <!-- Statistics Section -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Insertion at beginning"
            android:textStyle="italic" />
        <TextView
            android:id="@+id/statSummary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:padding="8dp"
            android:text="Performance stats will appear here"
            android:textColor="#333333"
            android:textSize="14sp" />
        <!-- Add this below your statistics TextView -->
        <TextView
            android:id="@+id/searchPerformance"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:padding="8dp"
            android:background="#EEEEEE"
            android:textColor="#333333"
            android:textSize="14sp"
            android:text="Search Performance Comparison will appear here" />
    </LinearLayout>
</ScrollView>